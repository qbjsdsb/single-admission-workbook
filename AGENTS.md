# AGENTS.md

## 项目定位

这是体育单招文化课语文、政治习题册的 LaTeX 模板工程，不是题库存档，也不是官方真题仓库。

## 公开仓库边界

- 不上传邮箱附件、调查报告原件、扫描试卷、个人信息或未经授权的题目原文。
- 不把本地收集资料直接复制到公开目录。
- 真实题目进入正式项目之前，必须经过来源登记和教师审核。
- 公开仓库只保留模板、空白样章、构建脚本和通用规则。

## 构建规则

- 默认使用 XeLaTeX 编译中文。
- 构建入口为 `scripts/build.ps1`。
- 输出写入 `build/`，不得覆盖 `src/` 或其他冻结目录。
- 失败时先读取日志中的第一处可操作错误，不凭猜测连续改写。
- 交付前检查 PDF 是否存在、页数是否合理、是否有乱码、缺字、溢出或空白页。

## 版式规则

- 采用克制的教材型黑、灰层级，不使用装饰性大色块。
- 题号保持醒目但不过大；题目中的括号就是答题位置，不单独增加底部答题条。
- 加点字、划线句子必须在源文件中有明确且可复核的标记。
- 学生版不放多余的解释性提示；答案、解析和教学说明放到教师版。
- B5、页边距、字体、行距和答题空间必须在样章中先确认。

## 验收流程

1. 先检查源文件和变更范围。
2. 编译 XeLaTeX。
3. 检查编译日志、页数和文本标记。
4. 渲染彩色页面并逐页查看。
5. 渲染灰度页面并逐页查看。
6. 发现版式问题时优先修模板，不在每道题上手工补丁。


