# AGENTS.md

## 项目定位

这是普通高等学校运动训练、武术与民族传统体育专业招生文化考试语文、数学、政治、英语四科习题册与出版基础设施，不是题库存档，也不是官方真题仓库。

## 工作边界

- 本地历史资料只读。不得删除、移动、覆盖、批量重命名或把本地绝对路径写成公开依赖。
- 不上传邮箱附件、调查报告原件、扫描试卷、个人信息、商业教辅正文或未经授权的题目原文。
- 真实题目进入正式项目之前，必须有文件级 provenance、题目级来源、题面/答案/解析分项状态和允许用途。
- 回忆版、模拟题、讲义和知识总结必须与 `official_exam` 分开。
- 任何语义修改都要保留原值、问题、建议修正和证据。只修排版不能悄悄改变题意。

## 内容与数据规则

- 题目 ID 是不可变逻辑身份，不依赖章节、当前题号或排版位置。
- 学生版、教师版、答案版和解析版必须从同一份 normalized content 生成。
- 学生版隐藏内部来源、审核状态和解析；教师版可以显示答案、解析、来源和教学提示。
- 公开 fixtures 只使用自拟文本或明确允许的公版短文本，目标是破坏版式，不代表教学结论。
- 政治材料必须记录 `event_date`、`source_date`、`valid_as_of`、`stale_after` 和 `temporal_scope`。

## 构建规则

- 默认使用 XeLaTeX；公共层、学科 profile 和内容生成层分离。
- 构建入口为 `scripts/build.ps1`，生成内容和 PDF 只写入 `build/`。
- 失败时先读取日志中的第一处可操作错误，不凭猜测连续改写。
- 在没有 XeLaTeX 的环境中只能报告“未编译”，不能把静态检查当成 PDF 通过。

## 版式规则

- 默认 `print-profile: b5-workbook`，但内容层不得写死 B5；未来允许 A4 proof、A4 print 和 screen proof。
- 采用克制的教材型黑、灰层级，不使用装饰性大色块。
- 题号醒目但不过大；括号直接承担答题功能，不自动增加重复答题条。
- 语文着重号必须是字下着重号，不得使用把点放到文字上方的 `\overset` 伪实现。
- 数学公式、英语西文和 IPA 由各自 profile 管理；不要用一个万能中文宏包硬套四科。

## 验收流程

1. 检查源文件、变更范围和禁止内容。
2. 运行内容/schema 静态检查。
3. 生成学生版和教师版并确认题目 ID 集合一致。
4. 编译 XeLaTeX，检查日志、页数、缺字、overfull 和空白页。
5. 渲染彩色与灰度页面，查看关键页；必要时逐页查看。
6. 用独立 QA 记录 Q0–Q8 gate，不把“编译成功”当成“出版通过”。
7. 发现版式问题时优先修组件和 profile，不在每道题上手工打补丁。


